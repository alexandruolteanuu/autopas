node_modules/
.next/
.env.local
.env
.DS_Store
# Doar folderul din rădăcină, cu feed-urile CSV descărcate de la sursă.
# Bara din față e obligatorie: fără ea, tiparul prinde ORICE folder numit
# `import` — inclusiv `lib/import/` (motorul de import) și rutele
# `app/api/import/`, care ar fi rămas necomise fără să spună nimeni nimic.
/import/

# fișiere temporare de depanare, scrise în rădăcină ca să vadă node_modules
dbg-temp.mjs
dbg*.mjs
